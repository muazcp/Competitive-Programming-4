#include<bits/stdc++.h>
using namespace std;

void solve() {
     int a, i;
     cin >> a >> i;
     cout << a * (i - 1) + 1 << "\n";
}

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    solve();
    return 0;
}