#include<bits/stdc++.h>
using namespace std;

void solve() {
    int n;
    cin >> n;
    vector<string> name(n);
    // Some code goes here
    int query;
    cin >> query;
    while(query--) {
        int p;
        cin >> p;
        // Some code goes here
    }
}

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    solve();
    return 0;
}